jobs:
  build:

    runs-on: windows-latest   # alternate runner OS is: ubuntu-latest

    steps:
    - name: Install Power Platform Tools
      uses: microsoft/powerplatform-actions/actions-install@v1

    - name: Export Solution
      uses: microsoft/powerplatform-actions/export-solution@v1
      with:
        environment-url: secrets.DEPLOYMENT_APPSOURCEURL
        user-name: secrets.DEPLOYMENT_APPID
        password-secret: ${{ secrets.DEPLOYMENT_APPSECRET }}
        solution-name: BasicRealEstate
        solution-output-file: 'BasicRealEstate.zip'
        working-directory: 'out'

    - name: Unpack Solution
      uses: microsoft/powerplatform-actions/unpack-solution@v1
      with:
        solution-file: 'out/BasicRealEstate.zip'
        solution-folder: 'out/solutions/BasicRealEstate'
        solution-type: 'Unmanaged'
        overwrite-files: true

    - name: Publish Solution
      uses: microsoft/powerplatform-actions/publish-solution@v1
      with:
        environment-url: secrets.DEPLOYMENT_APPSOURCEURL
        user-name: secrets.DEPLOYMENT_APPID
        password-secret: ${{ secrets.DEPLOYMENT_APPSECRET }}

    - name: Prepare solution changes for check-in into source control
      uses: microsoft/powerplatform-actions/branch-solution@v1
      with:
        solution-folder: 'out/solutions/BasicRealEstate'
        solution-target-folder: 'src/solutions/BasicRealEstate'
        token: ${{ secrets.DEPLOYMENT_BRE_TOKEN }}
